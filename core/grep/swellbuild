pkg_fetch() {
    fetch_url "$(cat sources)"
}

pkg_unpack() {
    unpack grep-*.tar.xz
}

pkg_build() {
    ./configure --prefix=/usr
    make -j$(nproc)
}

pkg_install() {
    make DESTDIR="$DESTDIR" install
}
