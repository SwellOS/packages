pkg_fetch() {
    fetch_url "$(cat sources)"
}

pkg_unpack() {
    unpack main.tar.gz
    mv sbpm-* sbpm
}

pkg_build() {
    cd sbpm
    cargo build --release
}

pkg_install() {
    cd sbpm
    install -Dm755 target/release/sbpm "$DESTDIR/usr/bin/sbpm"
}
