pkg_fetch() {
    fetch_url "$(cat sources)"
}

pkg_unpack() {
    unpack dxvk-*.tar.gz
}

pkg_build() {
    : # pre-built binary package
}

pkg_install() {
    mkdir -p "$DESTDIR/usr/lib/wine"
    cp -rv dxvk-*/x64/*.dll "$DESTDIR/usr/lib/wine/"
}
