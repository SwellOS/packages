pkg_fetch() {
    fetch_url "$(cat sources)"
}

pkg_unpack() {
    unpack master.tar.gz
}

pkg_build() {
    cargo build --release --manifest-path Cargo.toml
}

pkg_install() {
    install -d -m755 "$DESTDIR/usr/bin"
    install -m755 target/release/supm "$DESTDIR/usr/bin/supm"
}
