pkg_fetch() {
    fetch_url "$(cat sources)"
}

pkg_unpack() {
    unpack dash-*.tar.gz
}

pkg_build() {
    ./configure --prefix=/usr --bindir=/bin
    make -j$(nproc)
}

pkg_install() {
    make DESTDIR="$DESTDIR" install
}
