pkg_fetch() {
    fetch_url "$(cat sources)"
}

pkg_unpack() {
    unpack linux-*.tar.xz
}

pkg_build() {
    make mrproper
    make headers
}

pkg_install() {
    find usr/include -type f ! -name '*.h' -delete
    cp -rv usr/include "$DESTDIR/usr"
}
