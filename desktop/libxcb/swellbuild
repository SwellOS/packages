pkg_fetch() {
    fetch_url "$(cat sources)"
}

pkg_unpack() {
    unpack libxcb-*.tar.xz
}

pkg_build() {
    ./configure --prefix=/usr \
        --disable-static \
        --without-doxygen

    make -j$(nproc)
}

pkg_install() {
    make DESTDIR="$DESTDIR" install
}
