pkg_fetch() {
    fetch_url "$(cat sources)"
}

pkg_unpack() {
    unpack cmake-*.tar.gz
}

pkg_build() {
    ./bootstrap --prefix=/usr \
        --parallel=$(nproc) \
        -- -DCMAKE_BUILD_TYPE=Release

    make -j$(nproc)
}

pkg_install() {
    make DESTDIR="$DESTDIR" install
}
