pkg_fetch() {
    fetch_url "$(cat sources)"
}

pkg_unpack() {
    mkdir -p steam-extract
    cd steam-extract
    ar x "$SRCDIR/steam.deb"
    tar xf data.tar.xz
}

pkg_build() {
    : # binary package, no build needed
}

pkg_install() {
    cp -rv steam-extract/usr/* "$DESTDIR/usr/"
}
