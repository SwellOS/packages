pkg_fetch() {
    fetch_url "$(cat sources)"
}

pkg_unpack() {
    unpack findutils-*.tar.xz
}

pkg_build() {
    ./configure --prefix=/usr --localstatedir=/var/lib/locate
    make -j$(nproc)
}

pkg_install() {
    make DESTDIR="$DESTDIR" install
}
